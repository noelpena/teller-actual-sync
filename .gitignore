# Dependencies
/node_modules

# Runtime data directories (created by application)
/transaction-data
/logs
/actual-data

# Configuration files with secrets
config.json
/config/config.json

# Certificates (contain sensitive keys)
/certs/*
/certs/*.pem

# Environment files (contain credentials)
.env
.env.*
.env.hide